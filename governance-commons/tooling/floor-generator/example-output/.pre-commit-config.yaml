# Generated by the governance-commons floor generator (Stage 5, unpinned cut).
# Stack: python, typescript, terraform, alerting. Do not edit by hand; regenerate from the L1 layer.
# Local hooks invoke tools through the project runner; a pinning pass will
# switch to pinned upstream hook revisions.
repos:
- repo: local
  hooks:
  - id: bandit
    name: bandit (sast) [pinned bandit 1.9.4]
    entry: uv run bandit -q -r . -c pyproject.toml
    language: system
    pass_filenames: false
  - id: eslint
    name: eslint (lint) [pinned eslint 10.5.0]
    entry: npx --no-install eslint .
    language: system
    pass_filenames: false
  - id: gitleaks
    name: gitleaks (secrets) [pinned gitleaks 8.30.1]
    entry: gitleaks protect --staged --redact
    language: system
    pass_filenames: false
  - id: ruff-check
    name: ruff-check (lint) [pinned ruff 0.15.17]
    entry: uv run ruff check
    language: system
    pass_filenames: false
  - id: ruff-format
    name: ruff-format (format) [pinned ruff 0.15.17]
    entry: uv run ruff format --check
    language: system
    pass_filenames: false
  - id: tflint
    name: tflint (lint) [pinned tflint 0.63.1]
    entry: tflint
    language: system
    pass_filenames: false
